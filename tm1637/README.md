# راه‌اندازی نمایشگر ۷ سگمنت با TM1637

این پروژه یک کتابخانه‌ی سبک برای کنترل نمایشگر ۴ رقمی ۷ سگمنت با استفاده از تراشه‌ی **TM1637** و میکروکنترلر STM32 (با HAL) ارائه می‌دهد.

---

## 📌 امکانات

- پیکربندی ساده با استفاده از پایه‌های دلخواه
- کنترل شدت روشنایی (از ۰ تا ۷)
- نمایش اعداد صحیح و اعشاری
- کنترل مستقیم سگمنت‌ها
- قابلیت نمایش یا حذف صفرهای ابتدایی
- حالت تست (پر شدن همه سگمنت‌ها)

---

## 📘 معرفی توابع

### `tm1637_init`

```c
void tm1637_init(tm1637_t *tm1637, GPIO_TypeDef *gpio_clk, uint16_t pin_clk, GPIO_TypeDef *gpio_dat, uint16_t pin_dat);
```
راه‌اندازی اولیه‌ی نمایشگر با تعریف پایه‌های CLK و DIO.

tm1637: اشاره‌گر به ساختار نمایشگر

gpio_clk و pin_clk: پورت و پین مربوط به CLK

gpio_dat و pin_dat: پورت و پین مربوط به DATA

tm1637_brightness
void tm1637_brightness(tm1637_t *tm1637, uint8_t brightness_0_to_7);

تنظیم شدت روشنایی نمایشگر (مقدار بین ۰ تا ۷)


tm1637_write_segment

void tm1637_write_segment(tm1637_t *tm1637, const uint8_t *segments, uint8_t length, uint8_t pos);


نوشتن مستقیم اطلاعات سگمنت‌ها روی نمایشگر.

segments: آرایه‌ای از داده‌های سگمنت

length: تعداد رقم‌هایی که باید نمایش داده شوند

pos: موقعیت شروع (بین ۰ تا ۳)



tm1637_write_int
void tm1637_write_int(tm1637_t *tm1637, int32_t digit, uint8_t pos);

نمایش عدد صحیح روی نمایشگر.

digit: عدد موردنظر

pos: محل شروع نمایش


tm1637_write_float
void tm1637_write_float(tm1637_t *tm1637, float digit, uint8_t floating_digit, uint8_t pos);
مایش عدد اعشاری.

digit: عدد اعشاری مورد نظر

floating_digit: تعداد رقم اعشار

pos: محل شروع نمایش


tm1637_show_zero
void tm1637_show_zero(tm1637_t *tm1637, bool enable);
نمایش یا عدم نمایش صفرهای ابتدایی.

true: نمایش صفرهای ابتدایی

false: حذف صفرهای ابتدایی


tm1637_fill
void tm1637_fill(tm1637_t *tm1637, bool enable);

نمایش عدد ۸ روی همه‌ی سگمنت‌ها (برای تست)

true: پر کردن

false: پاک کردن نمایشگر

🛠 نمونه استفاده
tm1637_t display;
tm1637_init(&display, GPIOB, GPIO_PIN_6, GPIOB, GPIO_PIN_7);
tm1637_brightness(&display, 5);
tm1637_write_int(&display, 123, 0);
tm1637_write_float(&display, 23.5, 1, 0);














